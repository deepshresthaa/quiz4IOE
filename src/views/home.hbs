<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>quiz4IOE</title>
    <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
    <style>
      body { font-family: Arial, sans-serif; margin: 0; padding: 0; background:
      linear-gradient(to bottom, #2a3d72, #240046); color: white; text-align:
      center; } .header { padding: 2em; } .header h1 { font-size: 2.5rem;
      margin-bottom: 1rem; } .header p { font-size: 1.2rem; margin-bottom: 2rem;
      } .btn { padding: 0.8em 1.5em; background-color: #2a3d72; color: white;
      border: none; border-radius: 5px; cursor: pointer; font-size: 1rem; }
      .btn:hover { background-color: #2a3d72; } .categories { display: flex;
      flex-wrap: wrap; justify-content: center; gap: 1rem; margin-top: 2rem; }
      .category { background-color: rgba(255, 255, 255, 0.2); border-radius:
      10px; padding: 1rem; text-align: center; width: 120px; transition:
      transform 0.3s ease; } .category:hover { transform: scale(1.1); }
      .category img { width: 50px; height: 50px; } .category p { margin-top:
      0.5rem; font-size: 1rem; } .category a { text-decoration: none; color:
      inherit;  }
    </style>
  </head>
  <body>
    {{>navbar}}
    <div class="header">
      <h1>Unleash Your Inner Wizard of Wisdom</h1>
      <p>Embark on a Journey of Knowledge Exploration with Our Extensive
        Collection of Interactive Quizzes.</p>
      {{! <button class="btn">Get it now</button> }}
    </div>

    <div class="categories">
      <!-- Dynamically Render Categories -->
      {{#each categories}}
        <div class="category">
          <a href="{{this.link}}" target="_blank">
            <img src="{{this.icon}}" alt="{{this.name}}" />
            <p>{{this.name}}</p>
          </a>
        </div>
      {{/each}}
    </div>
  </body>
</html>